.next/
node_modules/
db/migrations/
# Agent run output. Generated data, not source: it is written by scripts/agents/run-agents.ts and
# read as a diff, and reformatting it would only add noise to that diff.
data/agents/
# Generated registries, written wholesale by scripts/background/build-*.ts. The four the corpus
# reads are version-controlled so they ship with the code; reformatting eighteen megabytes of
# generated JSON would rewrite every line of every rebuild and tell a reviewer nothing.
data/registries/
# Generated audit measurements, written wholesale by scripts/audit/denial-corpus/measure.ts. The
# whole point of the file is that its bytes change only when the corpus does, so reformatting it
# would put a diff in front of a reviewer that means nothing.
data/audits/
package-lock.json
test-results/
playwright-report/
scripts/seed-data/enriched-batch-19.ts
scripts/seed-data/enriched-batch-20.ts
scripts/seed-data/enriched-batch-27.ts
scripts/seed-data/enriched-batch-28.ts
scripts/seed-data/enriched-batch-29.ts
scripts/seed-data/enriched-batch-30.ts
# Generated retrieval measurements, written wholesale by scripts/semantic/benchmark.ts. Same reason
# as data/audits/: the bytes are the measurement, and reformatting them would put a diff in front of
# a reviewer that says nothing about the corpus. The Markdown copy is still scanned by
# scripts/quality/slop-scan.mjs, which is the check that matters for it.
docs/audits/semantic/
# Local pytest artefact, not source and not committed.
.pytest_cache/
# Phase measurement and capture output, written wholesale by the corpus-20k, design-study,
# trial-results and biohacker-pivot scripts: overlap runs, fold summaries, HTML audits, browser
# checks, identity maps, captured third-party pages and their manifests, and the resume ledgers the
# state modules rewrite on every batch. Same reason as data/agents/ and data/audits/ above — the
# bytes are the measurement, a rewrite would land at the next run anyway, and reformatting them puts
# a diff in front of a reviewer that says nothing about the corpus.
data/biohacker-pivot/
data/corpus-20k/
data/design-study/
data/trial-results/
# Running logs, rewritten at the top on every batch and hand-wrapped at 100 columns. Their reader
# check is scripts/quality/slop-scan.mjs, which reads the prose; realigning their tables would
# rewrite files that the next batch rewrites again.
docs/worklogs/
