import type { SeedDossier } from '@/lib/seed-types'

// Placeholder until this group's dossiers are written. An empty array keeps the loader honest:
// a group with no researched dossiers contributes no pages rather than fabricated ones.
export const MRNA_DOSSIERS: SeedDossier[] = []
