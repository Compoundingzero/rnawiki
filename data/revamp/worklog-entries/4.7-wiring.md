### [2026-09-06T02:42:00Z] Phase 4 — the blocks wired into the site and every page re-rendered

Commands: `npx tsx scripts/revamp/page_text_v5.ts`; `npx tsx scripts/with-disposable-database.ts --
npx tsx scripts/corpus-20k/load/materialise.ts --tier 1 --revamp --batches 2 --no-checkpoint`;
`.venv-corpus/bin/python -m pytest tests/test_render_safety.py`; `npm run gate`.

Output: `db/migrations/0026_corpus_revamp.sql` (page_registration, page_interactions, page_patent,
page_controlled, page_sections, page_display_names; `corpus_pages.controlled`; `page_relations.note`;
the suppression guard extended so a controlled page cannot hold a dose, bioavailability, n-of-1 or
time-to-signal block or seed), six components under `components/dossier/corpus/`,
`data/revamp/render-v5/{text,provenance,pages-all.ndjson,summary.json}`.

Result: 28,832 pages rendered from the page-blocks rebuilt at 08:32 (the 08:17 render read the
earlier ones and quoted label sentences those blocks no longer held); all 28,832 carry a
registration block, a patent line and a checked-sources statement; 3,615 interaction lines, 18,640
computed sections, 3,035 form-of notes, 1,836 controlled pages with 1,141 register-written dose
lines withheld on 390 of them; 1,130,608 sentences, every one with a field behind it. A predicted
line now names its counterpart and labels a bare ATC classification code. 13 pytest and 34 Vitest
render-safety cases pass; `npm run gate` PASSED (typecheck, lint, copy, format, drizzle-kit check,
2,593 unit tests, 167 integration tests on a disposable database, production build, 30 Playwright
tests).
