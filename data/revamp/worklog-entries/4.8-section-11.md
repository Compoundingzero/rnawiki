### [2026-09-07T07:56:00Z] Phase 4 §11 — furniture, the ruler's numerator, provenance of an absence, DOM parity, biosimilars

Commands: `identity_apply.py`; `build_blocks.py --relations …/relations-v3.parquet`;
`tier3_sections.py`; `page_blocks.py`; `page_text_v5.ts` and `page_text_v5.ts --with-furniture`;
`db/migrate.ts` and `materialise.ts --tier {1,3,2} --revamp --thresholds
data/revamp/thresholds-v5.json` on a disposable database; `next dev -p 3177`;
`dom_parity.py --base-url http://127.0.0.1:3177 --sample 200`;
`.venv-corpus/bin/python -m pytest tests/test_render_safety.py`; `npm run typecheck`,
`npm run lint`, `npm run test:unit`, `npx next build`, `npm run gate`.

Output: `db/migrations/{0029_page_seed_slots,0030_page_question_values}.sql`;
`data/revamp/identity/{trial-reassignments-v4.csv,relations-v3.parquet,apply-summary.json}`;
`data/revamp/{blocks,page-blocks,tier3-sections.parquet}`;
`data/revamp/render-v5/{text,provenance}` (furniture-free) and
`data/revamp/render-v5-with-furniture/{text,provenance}`;
`data/revamp/render-v5/dom-parity.json`.

Result. **Furniture** (§11): the absence rows render as one table under a caption carrying "as of"
once, each row `data-furniture="true"`; the checked-sources statement and the register names it
lists, the patent no-record line and the S10-only line carry it too. 350,910 furniture lines over
28,832 pages; the measured text is 12,473,782 words (mean 432.6) against 16,183,745 (mean 561.3)
with furniture — the with-furniture render is written by `--with-furniture`,
`rendered_dup_check.py` skips `[data-furniture]` beside `.cd-supervision`, and `derive_threshold.py`
reports the with-furniture figure beside the gate figure when given `--with-furniture-text-dir`.
**Provenance of an absence** now names only paths the record does not carry, beside the register and
the date it was read (`searched and not recorded: …; register: … as of …`), and a combination page's
component line names the component's own record (`… · record <key>`); 1,319,671 sentences, every one
with a trace, and every trace resolves — asserted over all 28,832 pages for the absence rule and
over a seeded 1,000-page sample for every trace class. **DOM parity**: 200 pages, 0 lines the render
lacks, 0 lines the page lacks, 0 out of order. Nine causes fixed: the empty-target supervision
sentence and the jurisdiction order (both from `jsonb` key order, now ordered by count and by §2's
jurisdiction order), seed rows whose parts were concatenated in storage order, the withdrawn arc's
position, the disambiguated display name, the neighbour counterpart the page never fetched, the
controlled-substance line-level dose withholding the page did not apply (Operating Rule 9 — the page
painted dose text the render withheld), and the slot values and sources the loader did not store
(migrations 0029 and 0030). **Loader**: `--thresholds` reads per-tier thresholds and
`presence-applicable-*.ndjson` beside them; Tier 1 48 and Tier 2 1 indexable at 23 and 20
present-and-applicable fields, Tier 3 none — the same numerator the ruler used, and
`present_applicable_count` is stored. **Biosimilars**: the FDA Purple Book decides which suffixed
page is one — 81 of 158 are licensed under 351(k) against the page they point at and open with "X is
a biosimilar of Y"; the other 77 are 351(a) biologics in their own right and keep the note Phase 3
wrote. 831 studies naming only the reference product moved to the reference page
(`R14c-REFERENCE-PRODUCT-TRIAL`), 13,151 reassignment rows in all. **Slop draw scope**: test (b)
applies to answer, derived, computed and hub sentences only, and `report.json` counts every sentence
it was not applied to under `failBExclusions`. Every check reads `canonical-v3`.

`derive_threshold.py` did not import: the `--with-furniture-text-dir` block declared
`CACHE_PREFIX` global a second time inside `main`, which is a syntax error. Fixed; the flag parses
and the with-furniture figure is reported beside the gate figure. The v6 re-derivation itself is
measure 2's step and this step did not run it: the local build was loaded from
`thresholds-v5.json`, whose per-tier shape is the one the loader reads.

`npm run build` had been failing with `Next.js build worker exited with code: null and signal:
SIGKILL` and no error. Cause, measured: Tailwind v4's automatic source detection walks the project
from `app/globals.css`, and `data/` is seventeen gigabytes of version-controlled measurement output;
the same tree built clean in two isolated worktrees whose `data/` holds only what HEAD carries.
`@source not '../data'` and `@source not '../.dvc'` bound the scan and the build passes.
`prettier --check .` could not read the DVC cache at all ("Invalid string length"), which
`.prettierignore` now excludes.

GATE G4 (§11 half): 16 pytest cases and 2,601 Vitest cases pass; typecheck, lint and the production
build pass. `npm run gate` FAILS at `format` on five files this step does not own —
`components/hubs/{HubMembers,HubSynthesis}.tsx`, `tests/unit/hubs-render.test.ts` and
`data/release/rnawiki-corpus-2026-09-06/{README,LICENSES}.md` — which are the hub and release steps'
to format.
