### [2026-09-05T05:29:41Z] Phase 0 Step 0.3 — baseline against the live site, same ruler

Commands, in order (all from the repository root, Python `.venv-corpus/bin/python`):

```
scripts/revamp/baseline_lists.py                                   # union.tsv + live sitemap read
scripts/corpus-20k/gate2/fetch.py --base https://rnawiki.com --slugs data/revamp/baseline/lists/union.tsv \
  --out data/revamp/baseline/html-text/live-dossiers.ndjson --concurrency 4 \
  --log data/corpus-20k/legal/requests.log
scripts/revamp/baseline_sets.py --fetched data/revamp/baseline/html-text/live-dossiers.ndjson \
  --out data/revamp/baseline
scripts/corpus-20k/overlap/measure.py --pages data/revamp/baseline/pages/<set>.ndjson \
  --out data/revamp/baseline/runs/<set> --exhaustive [--no-controls]   # draw803, draw324,
                                                                      # matched604, matched252,
                                                                      # indexed, indexed-prose
scripts/corpus-20k/gate2/folds.py --pages data/revamp/baseline/pages/indexed.ndjson \
  --out data/revamp/baseline/runs/indexed-folds-324 --fold-size 324
npx tsx scripts/corpus-20k/gate2/browser-checks.ts --base https://rnawiki.com \
  --slugs data/revamp/baseline/lists/samples.tsv --out data/revamp/baseline/browser-checks-live.json
scripts/revamp/baseline_log_browser_requests.py --checks data/revamp/baseline/browser-checks-live.json
scripts/revamp/baseline_production.py --out data/revamp/baseline/production.json
scripts/revamp/baseline_assemble.py
```

Output: `data/revamp/baseline.json`, `data/revamp/baseline-vs-final-report.md`, run artefacts under
`data/revamp/baseline/` (fetch, per-set pages, six scorer runs, the 324-page folds, the Playwright
run, the production read, the live sitemap copies).

The four corpus-20k measurement scripts ran unchanged. The URL set is the Phase 5 measurement's own
1,592-row fetch record rebuilt row for row (`data/corpus-20k/final/html-text/live-dossiers.ndjson`),
the set key lists are `data/corpus-20k/final/lists/*.txt`, and one NDJSON per set was written rather
than passing `--sample`, which truncates a COMBO key at its first comma. 1,592 of 1,592 URLs
answered 200 at concurrency 4 in 197 s; 1,616 request lines were appended to
`data/corpus-20k/legal/requests.log` (1,592 pages, 5 sitemaps, 19 browser navigations).

**All 69 compared figures reproduce the FINAL REPORT** — positional and lexical medians at both
draws and on the indexed set to six decimals, shared-word share 0.206851 on the 604 medicines,
crawl text-to-HTML 0.10649 on the indexed set, indexed 610 / 26 / 0 per tier read from the live
sitemap children and confirmed against `corpus_pages`, and every seed firing count corpus-wide and
on the indexable set from the CA-pinned read-only production read. Two provenance notes are recorded
in `baseline.json` → `discrepancies`: the FINAL REPORT's 3.3–5.5 % live text-to-HTML band was
measured pre-deploy on localhost over seven samples, and this run reproduces that band live over all
eight (0.033–0.055 across both widths, 0.038–0.055 at desktop, median 0.0455); and the report's
before/after table labels its two positional rows as the 803 and 324 draws while the after values
are the like-for-like 604 and 251 sets — the seeded draws themselves measure 0.300 and 0.275, and
both readings are stored separately.

GATE G0: no verdict from this step alone; 0.3's own condition (baseline matching the FINAL REPORT)
is met.
