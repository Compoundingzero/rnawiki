# Phase 7 brief (to be launched after Gate G4 is recorded)

Work in /Users/admin/ClaudeRepo/Claude Projects/Project RNAwiki/RNAwiki-corpus-completion on branch revamp/2026-09. Read docs/specs/revamp-2026-09.md Operating Rules and Phase 7; docs/specs/deployment-plan.md (the Phase 7 run-book is binding; the §6.1 document architecture and the PORT/proxy consequence); docs/specs/ci-checks.md; data/revamp/baseline.json and data/revamp/worklog-entries/0.3.md (the exact baseline commands and sets); data/revamp/thresholds-v14.json; data/revamp/identity/{redirect-plan-v5.csv,trial-reassignments-v5.csv,duplicate-holds.csv}; data/revamp/hubs/load/; the last 120 lines of docs/worklogs/revamp-2026-09.md. Python at .venv-corpus/bin/python. No single Bash call over 90 seconds (nohup + poll). Never print the production database URL; production access only over the CA-pinned connection (sslmode=verify-ca, sslrootcert=/Users/admin/ClaudeRepo/Claude Projects/Project RNAwiki/rnawiki-backups/railway/postgres-root.crt) with the Postgres service's DATABASE_PUBLIC_URL from `railway variables --service <postgres service> --json`. Every step writes its artefact path; a step without one did not happen.

Tier map: data/revamp/tiers/model-assignment-v3.ndjson (every loader and check reads it). Steps, in the run-book's order:
0. Backup: pg_dump -Fc to rnawiki-backups/revamp-2026-09-deploy/rnawiki-pre-revamp.pgcustom; bytes, sha256, table-data entry count; compare with the 2026-09-05 backup.
1. Code first: commit the working tree on revamp/2026-09 with a message naming the revamp (the lead commits bookkeeping; you commit nothing else — instead confirm `git status` is clean or list what is not), push, open the PR to main (title "Revamp 2026-09: corrected ruler, eighteen open sources, molecular identity, registration/interaction/patent/computed sections, hubs, server-written documents, CI checks"; body: the gate figures from state.json/worklog; last line "🤖 Generated with [Claude Code](https://claude.com/claude-code)"); `gh pr checks --watch` (release gate + revamp-checks; re-run a flaked job once; fix only what CI names); merge on green (`gh pr merge --merge`); watch the Railway deployment to SUCCESS; read the migration count after (33); set PORT behind the proxy if the document forward needs it (deployment-plan.md); verify one legacy-only slug answers 200; record the deployment id and the previous one for rollback.
2. Verify with the old rows: the eight samples 200 on the new renderer; scripts/corpus-20k/deploy/verify-live.ts --require-all; robots unchanged; frozen bar 0.00 px; JSON-LD one block; sitemap index children present.
3. Load tiers in order from the workstation: materialise.ts --production-confirmed --thresholds data/revamp/thresholds-v14.json applying redirect plan v5, trial reassignments v5, duplicate holds; Tier 1 → verify → Tier 2 → verify → Tier 3 → verify (after the 15-minute sitemap cache: verify-live --require-all; redirect_check.py --base-url https://rnawiki.com over all 9,857 legacy slugs; rendered_dup_check.py --base-url https://rnawiki.com --fail-on-indexable; 20 random pages per tier compared with the build output (h1, block count, first block); frozen bar; JSON-LD). Fingerprinted markers; a re-run is a no-op.
4. Hubs: hubs_load.ts --production-confirmed; 10 random hubs vs the build; link_graph_check.py --base-url https://rnawiki.com; hubs.xml count equals the surviving hub count.
5. IndexNow: npm run discovery:indexnow -- --tier 1, --tier 2, and the hubs child (add --hubs to the corpus-aware submitter if absent: it reads the sitemap child); SEO_DEPLOYMENT_ENV=production; ledger lines recorded.
6. Orphan audit: discovery:monitor over the whole index scoped per child; 0 unreachable; click depth ≤ 3 for every indexed record and hub.
7. After: data/revamp/after.json — every baseline.json measurement re-run by the same scripts and samples against the live site (positional 803/324 draws and like-for-like sets, shared-word share 604, crawl text-to-HTML on the indexed set, live text-to-HTML on the eight samples, indexed count per tier from the live sitemap, derived-section firing counts from production), plus the Phase 4–6 measures against the live site (thresholds v11 on the live text of the indexed set, rendered duplicates on the live indexable set and hubs, payload on the 108 pages, common-line share, median words); data/revamp/before-after.md in the corpus-20k FINAL REPORT layout with baseline vs after columns.
8. Run the post-deploy CI job (workflow_dispatch) and record its result.
Return JSON with every artefact path, every count, and every deviation from the run-book with its reason.
